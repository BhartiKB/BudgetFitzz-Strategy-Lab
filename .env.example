# Copy this file to .env and add credentials locally.
# Paid generation is blocked by project configuration.
GEMINI_API_KEY=
HF_TOKEN=

# Deliberately unused. Leave blank unless paid generation is explicitly approved.
FAL_KEY=
