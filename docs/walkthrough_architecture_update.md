# Walkthrough architecture update

The immutable source recording `Screen Recording 2026-07-18 122806.mp4` is preserved and is not overwritten. Its SHA-256 was verified before and after composition: `871395fa1e7f0f0819dc1a4d415346beb2a6214b309be3ed5fa822e2da5ef028`.

A 40-second silent, caption-led architecture section is inserted after 25 seconds. It uses captured local application states, current manifest categories, and concise excerpts from `app/server.py` and `app/app.js`. There is deliberately no audio stream, voice narration, text-to-speech, or music.

The final file is `submission/final/platform_walkthrough.mp4`; the deployment copy is `deploy/budgetfitzz_platform_walkthrough_with_architecture.mp4`. Representative frames are in `docs/demo_frames/`. Full probe metadata is stored in `logs/architecture_walkthrough.json`.
